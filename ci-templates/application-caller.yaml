# Copy this as .github/workflows/kubernetes-release.yaml in each application.
name: Kubernetes dev release

on:
  push:
    branches: [dev]

permissions:
  contents: read

jobs:
  release:
    uses: IDUclub/urban-assistant-deploy/.github/workflows/reusable-application-release.yaml@main
    with:
      # Replace with the exact key from services.yaml.
      service: idu-dvd
      # Required: replace this fail-closed placeholder with dependency setup
      # and the repository's real test suite. The image build never starts
      # unless this script succeeds.
      test-command: |
        echo "Configure dependency installation and tests for this repository" >&2
        exit 1
    secrets: inherit
