apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: scenarios-conductor
spec:
  selector:
    matchLabels:
      app.kubernetes.io/monitor: scenarios-conductor
  endpoints:
    - port: metrics
      path: /metrics
      interval: 30s
