apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: genbuilder
spec:
  selector:
    matchLabels:
      app.kubernetes.io/monitor: genbuilder
  endpoints:
    - port: metrics
      path: /metrics
      interval: 30s
