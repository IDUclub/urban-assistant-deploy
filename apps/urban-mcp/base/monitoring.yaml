apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: urban-mcp
spec:
  selector:
    matchLabels:
      app.kubernetes.io/monitor: urban-mcp
  endpoints:
    - port: metrics
      path: /metrics
      interval: 30s
