apiVersion: apps/v1
kind: Deployment
metadata:
  name: idu-dvd
  annotations:
    configmap.reloader.stakater.com/reload: idu-dvd-config
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: idu-dvd
  template:
    metadata:
      labels:
        app.kubernetes.io/name: idu-dvd
        app.kubernetes.io/component: api-mcp
    spec:
      enableServiceLinks: false
      automountServiceAccountToken: false
      containers:
        - name: app
          image: urban-assistant/idu-dvd:base
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: idu-dvd-config
            - secretRef:
                name: idu-dvd-secrets
          ports:
            - name: http
              containerPort: 8000
          volumeMounts:
            - name: runtime
              mountPath: /tmp/idu-dvd
          startupProbe:
            httpGet:
              path: /ping
              port: http
            failureThreshold: 60
            periodSeconds: 5
          readinessProbe:
            httpGet:
              path: /ping
              port: http
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /ping
              port: http
            initialDelaySeconds: 30
            periodSeconds: 20
          resources:
            requests:
              cpu: 500m
              memory: 1Gi
            limits:
              cpu: "2"
              memory: 4Gi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            seccompProfile:
              type: RuntimeDefault
      volumes:
        - name: runtime
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: idu-dvd
spec:
  selector:
    app.kubernetes.io/name: idu-dvd
  ports:
    - name: http
      port: 8000
      targetPort: http
---
apiVersion: v1
kind: Service
metadata:
  name: idu-dvd-dev
spec:
  type: NodePort
  externalTrafficPolicy: Cluster
  selector:
    app.kubernetes.io/name: idu-dvd
  ports:
    - name: http
      port: 8000
      targetPort: http
      nodePort: 31002
